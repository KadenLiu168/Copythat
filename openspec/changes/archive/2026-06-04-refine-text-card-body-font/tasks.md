## 1. Text Preview Typography

- [x] 1.1 Change the text card body preview to use a macOS system text font instead of the app-wide `CopythatFont` helper; verify by inspecting `ClipboardCardView.swift`.
- [x] 1.2 Reduce the text card body preview size to about 13pt, keep line spacing at about 1pt, and allow about 10 preview lines; verify by inspecting `ClipboardCardView.swift`.
- [x] 1.3 Confirm selected-card scale and raised positioning remain unchanged; verify by inspecting `ClipboardCardView.swift`.

## 2. Verification

- [x] 2.1 Run `swift build` and confirm the app builds.
- [x] 2.2 Run `./script/verify_all.sh` and confirm the verification suite passes.
