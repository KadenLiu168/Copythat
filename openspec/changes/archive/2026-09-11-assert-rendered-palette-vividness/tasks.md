# Tasks: assert-rendered-palette-vividness

## 1. Measurement helper

- [x] 1.1 Add a test-local OKLab conversion helper to the test target: calibrated `NSColor` → `usingColorSpace(.sRGB)` → linear sRGB → OKLab, exposing L, a, b and chroma. Verify with a self-check test that pure white and pure black yield chroma ≈ 0 (within 0.001) and that a fully saturated sRGB primary matches the published OKLab reference vector within 0.002. Derive that reference vector from the helper and confirm it against the published OKLab definition before recording it as an expected constant — do not type the constant from memory.
  - Implemented as a file-private `OKLab` struct (`l`, `a`, `b`, `chroma`) fed by `NSColor.renderedSRGB` → linearize → published linear-sRGB→LMS matrix → cube root → published LMS→OKLab matrix.
  - Self-check test: `oklabConversionMatchesPublishedReferenceValues` (white/black chroma < 0.001; pure sRGB red at L 0.627955 / a 0.224863 / b 0.125846 within 0.002).
  - Reference vector confirmed two ways: Ottosson's published constants were read from source rather than recalled, and an independent XYZ-route implementation (sRGB → XYZ D65 → published M1/M2) reproduces the published XYZ example table exactly (0.450/1.236/-0.019, 0.922/-0.671/0.263, 0.153/-1.415/-0.449) and agrees with the direct route on pure red within 3e-5.
- [x] 1.2 Assert the helper reports the six current palette tokens with the chroma values produced by the audit that motivated this change: amber 0.165, green 0.210, cyan 0.133, blue 0.174, violet 0.193, pink 0.196, each within ±0.002. Verify the test fails when a deliberately wrong matrix constant is substituted into the helper, confirming the assertion is not vacuous.
  - Test: `paletteChromaMatchesAuditedBaseline`; measured 0.1649 / 0.2100 / 0.1326 / 0.1743 / 0.1925 / 0.1962.
  - Non-vacuity: substituting `0.4100000000` for `0.4122214708` in the linear-sRGB→LMS matrix fails this test plus the self-check (3 issues). Perturbation reverted.

## 2. Rendered-space palette assertions

- [x] 2.1 Extend the palette test to read each token through `token.color.usingColorSpace(.sRGB)` and assert the rendered values: sRGB saturation ≥ 0.55, sRGB brightness ≥ 0.75, and OKLab chroma ≥ 0.11. Keep the existing exact per-token H/S/B target assertions unchanged as change detectors. Verify all six tokens pass against the current palette.
  - The two calibrated-space floors were removed rather than kept alongside, per the proposal's "move the floors into rendered space".
  - All six pass: rendered sRGB saturation 0.58–1.00, brightness 0.80–0.99, chroma 0.133–0.210.
- [x] 2.2 Extract the three rendered-space assertions into a reusable helper that takes a color, then add a regression-guard test that feeds the previously shipped under-vivid cyan (calibrated 0.52 / 0.60 / 0.82) through it and verifies the chroma floor rejects it (chroma ≈ 0.099 < 0.11). To confirm the chroma assertion is non-vacuous, verify the guard fails when the chroma floor assertion is removed — using a color that passes the rendered saturation and brightness floors but fails only chroma (calibrated hue 0.52, saturation 0.61, brightness 0.82; renders to sRGB saturation ≈ 0.56, brightness ≈ 0.85, chroma ≈ 0.101). The shipped 0.60 cyan cannot isolate the chroma floor because it also fails the rendered saturation floor (sRGB 0.5497 < 0.55). Record in the task notes that the old calibrated HSB floor accepted this same shipped color (calibrated saturation 0.60 ≥ 0.55, brightness 0.82 ≥ 0.75) — that is the regression the floor now catches.
  - Helper: `unmetRenderedVividnessFloors(of:)` over a `RenderedVividnessFloor` enum (`saturation` 0.55, `brightness` 0.75, `chroma` 0.11).
  - Guards: `renderedVividnessFloorsRejectPreviouslyShippedUnderVividCyan` and `chromaFloorRejectsColorThatPassesRenderedSaturationAndBrightness`.
  - Task note (the regression now caught): the old calibrated HSB floor accepted the shipped under-vivid cyan — calibrated saturation 0.60 ≥ 0.55 and brightness 0.82 ≥ 0.75 — which is why it passed all 86 tests and every verifier while rendering less colorful than the palette it replaced. The rendered floors reject it on saturation (0.5497) and chroma (0.0993).
  - Non-vacuity: with `.chroma` dropped from the checked floors, both guard tests fail (2 issues, the isolation color's shortfall set becomes empty). Perturbation reverted.
- [x] 2.3 Rename `colorTokensRoundTripAndShareSaturationAndBrightness` to a name describing the contract it now asserts (round-trip, distinct hues, rendered-space floors), and verify no other test, script, or document still references the old name.
  - Renamed to `colorTokensRoundTripKeepDistinctHuesAndMeetRenderedVividnessFloors`.
  - The only surviving references to the old name are frozen historical evidence under `openspec/changes/archive/` (deliberately left untouched) and this change's own proposal/tasks text.

## 3. Verification

- [x] 3.1 Run `swift build` and `./script/verify_all.sh` and confirm the full suite passes. In this environment use the SwiftPM shim: `PATH=/tmp/wb-swift-shim:$PATH ./script/verify_all.sh`.
  - Confirmed: the gate was run from a normal terminal session and completed with exit 0, with every stage green — the code-level verifiers (content keys, pasteboard write, paste target, paste decision, source resolution, source attribution timing, history performance at 5000 items, icon sizes), three `build_and_run.sh` rebuild-and-sign cycles, `portable app ok`, `panel ok {Height = 351; Width = 1642; X = 34; Y = 741}`, and `bundle ok local.copythat.clipboard LSUIElement True`. `swift test` = 92 tests / 17 suites (88 before this change + 4 new); exit 0 under `set -e` means no earlier stage failed.
  - Why the agent could not produce this record, for the next person who hits it: `build_and_run.sh` opens with `rm -rf dist/Copythat.app` (full rebuild every invocation; `verify_all.sh` invokes it three times), and the agent runtime's safe-delete guard — scoped to the sandboxed shell — blocks once a turn's cumulative deleted-file count would pass 50, while each rebuild deletes 27 files. A sandboxed turn therefore affords exactly one rebuild, against the three this gate needs.
  - Measured correction to an earlier claim in this task, which was wrong: the bundle is 35 entries / 27 files, not "54 entries"; 54 and 108 are the guard's per-turn cumulative figures (2 × 27 and 4 × 27). The apparent contradiction of the same value 54 being blocked in one turn and allowed in another was the sandbox bypass being active for those direct calls; three reproductions under the sandbox all blocked at 54.
  - Also corrected: `--verify-panel` failing with `Copythat panel did not appear in the window server` was an artifact of the sandboxed shell, not a missing GUI session — it passes with the sandbox bypass active and in a normal terminal, as the `panel ok` line above shows.
- [x] 3.2 Confirm no palette value changed: `git diff Sources/Copythat/Stores/AppSettings.swift` shows no modification to any `PinboardColorToken.color` value, and the rendered hex values for all six tokens still match the audited baseline (amber `#F7941D`, green `#26CD52`, cyan `#00CCE0`, blue `#2B8AEF`, violet `#975EE0`, pink `#FB61A1`).
  - `git diff Sources/Copythat/Stores/AppSettings.swift` is empty; the only modified file in the working tree is `Tests/CopythatTests/AppSettingsPinboardTests.swift`. Measured hex values match the baseline for all six tokens.
