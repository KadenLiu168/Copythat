## 1. V2 Persistence And Compatibility

- [x] 1.1 Add temporary-directory persistence tests for text, URL, image, file, pin, pinboard, source icon, link title, and link image round trips; verify exact runtime-visible values after reload.
- [x] 1.2 Implement v2 metadata DTOs and content-addressed immutable media blobs without changing `ClipboardItem`; verify the round-trip tests and confirm v2 JSON contains no raw media Base64.
- [x] 1.3 Add deterministic shared-media and image-heavy metadata-only tests with blob-write counts, byte counts, and unchanged blob identity/content; verify duplicate icons occupy one blob and a pin change creates and rewrites zero blobs.
- [x] 1.4 Add URL-preview-update tests, then implement blob-first, atomic-manifest-second writes; verify preview adds only its new blob while unrelated blobs remain unchanged.
- [x] 1.5 Add version-1 wrapper, raw legacy array, and legacy UserDefaults fixtures; implement explicit-version v2 load with per-load blob cache and verify all formats restore expected items without synchronous launch migration writes.
- [x] 1.6 Add fault-injected blob/manifest failure and missing-blob tests; verify failed commits leave the old manifest and referenced blobs intact, legacy preferences are not removed early, and missing media is not silently treated as healthy history.
- [x] 1.7 Add post-commit GC tests for unreferenced versus shared blobs; implement best-effort cleanup coordinated with writes and verify a GC failure does not turn a committed save into failure.

## 2. Scheduling And Quit

- [x] 2.1 Add controlled write-barrier tests for generations 1–4, stale arrival, failure/retry, and flush; verify actual commits coalesce to the in-flight and latest states, never regress, and flush waits for the latest attempt.
- [x] 2.2 Implement the single-drain coordinator and serial worker, retaining the store's injected persistence seam; verify no save encoding, hashing, or file IO occurs on the MainActor mutation path and existing store tests cannot write to the user's history.
- [x] 2.3 Wire production `ClipboardStore`/`AppModel` saves through the coordinator without changing capture or Link Preview behavior; verify add, Pin/Unpin, pinboard assignment/rename, remove, clear, and preview-enrichment saves with focused tests.
- [x] 2.4 Add termination tests for successful pending save, failed latest save, Retry, Quit Anyway, and Cancel Quit; implement AppKit `terminateLater`/reply handling and verify no success reply for an older generation or silent exit on failure.

## 3. Verification And Adversarial Review

- [x] 3.1 Run focused persistence and store tests, `swift test`, `swift build`, `./script/verify_all.sh`, and `git diff --check`; record exact results and distinguish environment failures from product failures. — Focused suites: persistence 13/13, store 3/3, coordinator 3/3; full suite 162/162; build, full verification script, and diff check passed. The script used the existing Conda Python because system Python lacks Pillow.
- [x] 3.2 Review data-loss paths—migration crash, manifest failure, immediate Quit, rapid Pin/Delete/Add, and preview arrival during a save—and verify last-good history plus latest-state retry through deterministic tests or explicit fault injection. — Verified by `failedVersionOneMigrationKeepsTheLegacyFileReadable`, `failedMigrationKeepsLegacyPreferencesUntilManifestCommit`, `blobAndManifestFailuresLeaveLastCommittedHistoryReadable`, `pendingLatestSaveCompletesNormalTermination`, `retrySavesTheLatestStateBeforeReplyingToQuit`, and `rapidMutationsAndPreviewArrivalCommitOnlyLatestSnapshot`.
- [x] 3.3 Review generation inversion, concurrent writer exclusion, GC versus pending snapshots, and premature flush completion; verify controlled concurrency tests reproduce and reject each unsafe ordering. — Verified by `serialWorkerExcludesConcurrentWritersAndRejectsLateOldGeneration`, `coordinatorDefersGarbageCollectionUntilLatestPendingSnapshotCommits`, `coalescesPendingGenerationsAndFlushWaitsForLatestCommit`, and `terminationWaitsForAnewerGenerationInsteadOfAcceptingAnOlderSave`.
- [x] 3.4 Review an image-heavy metadata-only mutation for off-MainActor work, zero existing-media rewrites, zero new blob bytes, and payload-free v2 manifest; verify the deterministic regression test rather than a timing threshold. — `imageHeavyPinMutationRunsWorkerWritesOffMainAndReusesEveryBlob` passed with off-main writes, no blob writes/bytes, unchanged blob identities, and no embedded media payloads.
- [x] 3.5 Run OpenSpec strict validation and inspect the final diff for unrelated Change 3, capture, UI, search, and paste edits; verify only this Change's intended scope remains. Do not archive, commit, or push without separate authorization. — `openspec validate reduce-history-persistence-write-amplification --strict` passed; final diff is limited to this Change's persistence, coordinator/worker, quit handling, tests, and artifacts. No archive, commit, or push performed.
