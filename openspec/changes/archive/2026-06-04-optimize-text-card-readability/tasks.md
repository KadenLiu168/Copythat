## 1. Text Card Presentation

- [x] 1.1 Update text card preview typography and spacing to show more readable content within the existing card size; verify by inspecting `ClipboardCardView.swift`.
- [x] 1.2 Remove the early text preview fade mask while preserving selected-card scale and offset behavior; verify by inspecting `ClipboardCardView.swift`.

## 2. Verification

- [x] 2.1 Run `swift build` and confirm the app builds.
- [x] 2.2 Run `./script/verify_all.sh` and confirm the verification suite passes.
