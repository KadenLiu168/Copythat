## 1. Remove Obsolete Image Processing

- [x] 1.1 Replace `ClipboardCardViewTests`' dependency on production `representativeColor` with a minimal test-local image-color assertion that still proves different cards expose their own red and blue captured source icons; verify with `swift test --filter ClipboardCardViewTests`.
- [x] 1.2 Remove `representativeColor`, `warmThemeColor`, `normalizedCardAccent`, and the complete `foregroundLogoCutout` orphan chain from `NSImage+PasteData.swift` while retaining both active PNG export paths unchanged; verify the removed symbols have no remaining source or test references and run `swift build`.

## 2. Remove Remaining Unused Helpers

- [x] 2.1 Remove `ClipboardItem.sample` and `CopythatIcon.transparentMark()` without changing their surrounding model and resource-loading behavior; verify both symbols have no remaining repository references and run `swift build`.
- [x] 2.2 Remove the `make_transparent_icon` alias and generate `AppIcon-transparent.png` through `make_app_icon(1024)` directly; verify its generated bytes match a pre-change baseline and `git diff` contains no generated asset changes.

## 3. Full Verification

- [x] 3.1 Confirm repository search finds none of the removed declarations or call sites, run `swift test` and verify the baseline remains 92 tests across 17 suites, then run `swift build`, `./script/verify_all.sh`, and `git diff --check` with no failures.
